
    // jQuery Ready Function waits for the document to complete loading before initiating JavaScript
    $(document).ready(function() {

     
        // Create a loop that generates a Random Number between 19-120.
        
        }
  
            // Add to the Random number.
            randomNumber = random + randomNumber;
          }
  
          // Then prepend the Random number to the top of our random-number div.
          $("#random-number").prepend("<br><hr>" + randomNumber);
        });   
        
        
        
        
        
        
        // When random-button (Crystal) is clicked...
      $("#random-button").on("click", function() {

        // ...create a string which will hold the Random number.
        var lottoNumber = "";

    });
